from setuptools import setup, find_packages


setup(name="controller",
      version='0.1',
      url="https://github.com/4Pi-R2/controller",
      author="Lily Daligault--Hardy",
      packages=['controller'],
      install_requires=['struct','typing','smbus'],
      license='MIT'
      )
